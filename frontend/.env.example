# Frontend build-time config.
# Leave VITE_API_BASE empty to use the Vite dev proxy (/api -> http://127.0.0.1:8080).
# In production set it to your deployed backend origin, e.g. https://taxassist.onrender.com
VITE_API_BASE=
